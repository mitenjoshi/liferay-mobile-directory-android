<resources>

    <style name="MPDSplashTheme" parent="android:style/Theme.Light">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:windowAnimationStyle">@style/IntroAnimation</item>
    </style>

    <style name="IntroAnimation" parent="android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/fade_in</item>
        <item name="android:activityOpenExitAnimation">@anim/fade_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/fade_in</item>
        <item name="android:activityCloseExitAnimation">@anim/fade_out</item>
    </style>

    <style name="MPDTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>

        <!-- textColorPrimary menu text color -->
        <item name="android:textColorPrimary">@color/black_70</item>
        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/primary</item>
        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/primary</item>

        <!-- colorControlNormal search view expanded -->
        <item name="colorAccent">@color/black_20</item>

        <!-- colorControlNormal menu icons -->
        <!-- colorControlNormal listview scrollbar -->
        <item name="colorControlNormal">@color/white</item>

        <!-- colorControlNormal listview pressed -->
        <item name="colorControlHighlight">@color/primary</item>
        <item name="colorSwitchThumbNormal">@color/emerald</item>
    </style>

    <style name="HorizontalDivider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">.75dp</item>
        <item name="android:background">@color/black_15</item>
    </style>

</resources>